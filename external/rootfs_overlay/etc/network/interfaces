auto lo
iface lo inet loopback

#auto eth0
#iface eth0 inet dhcp
#  hostname $(hostname)

#auto usb0
#iface usb0 inet dhcp
#  hostname $(hostname)

# wlan0 is configured at boot by /etc/init.d/S30wifi_mode (AP or STA).
